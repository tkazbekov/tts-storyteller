# VibeVoice backend dependencies
# Community fork implementation
git+https://github.com/vibevoice-community/VibeVoice.git

# Core dependencies (if not already in main requirements)
transformers>=4.40.0
torch>=2.0.0
torchaudio>=2.0.0
soundfile>=0.12.1
accelerate>=0.20.0
bitsandbytes>=0.41.0
